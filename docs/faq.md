# FAQ

## Why does a command return no output?
Use `--quiet` suppresses output. Remove it to see payloads.

## Why does a plugin fail to load?
Plugins are validated before activation. Check metadata and compatibility.

## How do I switch output format?
Use `--format json` or `--format yaml`.

## Where is config stored?
See reference/config_sources.md for sources and precedence.
