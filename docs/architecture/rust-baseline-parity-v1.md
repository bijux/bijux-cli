# Rust Baseline Parity v1

Date: 2026-03-09

## Milestone statement

Rust reached baseline parity for the first consolidated command set under the current parity runner and baseline gate:

- No command in the covered set is marked `python-only`.
- Baseline parity statuses are frozen in `docs/architecture/parity/baseline-parity-v1.json`.
- CI enforces no status regression for covered commands.

## Covered commands

See `docs/architecture/rust-parity-status-table.md`.
