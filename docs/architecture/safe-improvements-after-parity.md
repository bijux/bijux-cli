# Safe Improvements After Parity

Date: 2026-03-09

1. Improve text formatting and UX only when command exit behavior and machine output contracts remain unchanged.
2. Add richer diagnostics fields under additive schema evolution (no required field removals).
3. Expand plugin diagnostics once parity-covered command exits/streams remain baseline-stable.
4. Optimize performance only when parity status does not regress for covered commands.
5. Extend command coverage using the same gate workflow: capture, compare, decide, baseline update.
