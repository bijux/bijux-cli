# Known Remaining Parity Gaps

This list is generated from machine-readable evidence:
- `artifacts/status/status_known_parity_gaps.json`
- `artifacts/parity/command_parity_matrix.json`

Use `bijux dev cli status --format json` for current blockers, owners, and gap reasons.
