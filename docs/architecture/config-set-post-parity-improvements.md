# Config Set Post-Parity Improvements

These items are intentionally deferred until strict Python parity remains stable.

1. Add explicit cross-process lock contention tests when lock strategy is introduced.
2. Add configurable write conflict policy for concurrent writers.
3. Add richer diagnostics for write-path permission failures.
4. Add optional metadata output for mutation source and write strategy.
