# Next Crate Batch Decision

Date: 2026-03-09

## Input

Decision source is `artifacts/parity/rust_python_parity_report.json` generated by the unified parity runner.

## Decision

Next crate-focused batch: `bijux-cli-core` + `bijux-cli-output` for payload/text parity closure of covered commands.

## Why this batch

1. Current covered commands are all `rust-partial`, indicating output parity deltas rather than routing/exit hard failures.
2. Core and output crates own the largest remaining payload and presentation differences.
3. Closing these deltas yields the fastest path from partial to complete status across the current baseline set.
