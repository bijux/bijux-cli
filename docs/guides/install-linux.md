# Install on Linux

Use one install channel and verify the active binary immediately.

## Cargo

```bash
cargo install bijux-cli
bijux version
```

## Pip

```bash
python -m pip install --upgrade bijux-cli
bijux version
```

## Validation

```bash
bijux cli paths
bijux cli doctor
```

