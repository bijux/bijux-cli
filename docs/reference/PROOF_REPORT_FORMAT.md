# Proof Report Format

Human-readable proof report output should include:

- proof id
- run id
- status (`complete` or `incomplete`)
- completeness flag
- determinism evidence level
- integrity evidence level
- incomplete reasons (if any)

Example:

- proof id: `proof-run-123`
- status: `complete`
- determinism: `verified`
- integrity: `verified`
