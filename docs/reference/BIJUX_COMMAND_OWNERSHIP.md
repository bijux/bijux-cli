# Bijux Command Ownership

- root `bijux`: umbrella command composition and product routing
- `bijux dag`: computation-graph truth surfaces and operator contracts
- `bijux atlas`: deployment and operations overlay surfaces
- `bijux dna`: HPC and scheduler execution overlays

Rule: non-`dag` commands must not reinterpret `bijux dag` semantics.
