# Archived and moved root docs (tasks 21-40 cleanup)

These files were removed from the root index and moved from root-level entrypoint space under root-reduction tasks:

- Compatibility, ownership, and policy merge candidates
- Roadmap and generation artifacts
- Speculative and simulation-heavy explorations
- Historic federation/control-plane narrative split documents

These files remain available for historical context and can be removed in a later follow-up when all dependencies are retired.
