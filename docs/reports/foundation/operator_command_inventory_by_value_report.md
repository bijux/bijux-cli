# Operator Command Inventory by Value

Command classification source is the app command tree and operator contract tests.

| Classification | Commands |
| --- | --- |
| core | `validate`, `plan`, `run`, `runs`, `inspect`, `diff`, `replay`, `prove`, `verify`, `export`, `import`, `capabilities` |
| power | `explain-*`, `artifact-inspect`, `drift-report`, `recovery-*` |
| diagnostic | `inspect`, `explain`, `run-history`, `artifact-verify` |
| governance-support | command/report lockstep and docs/schema surface checks |
| experimental | modeled surfaces explicitly marked as non-default in docs/contracts |
