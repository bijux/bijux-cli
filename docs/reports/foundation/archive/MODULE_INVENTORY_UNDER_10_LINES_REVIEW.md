# Modules Under 10 Lines Review

Generated from Rust source files under crates/.

| Module | Lines | Review disposition |
| --- | --- | --- |
| `crates/bijux-dag-app/src/cache/mod.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-app/src/explain/cmd.rs` | 6 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-app/src/explain/mod.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-app/src/format/mod.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-app/src/graph/cmd.rs` | 6 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-app/src/graph/mod.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-app/src/graph/validate_cmd.rs` | 6 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-app/src/inspect/doctor_cmd.rs` | 6 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-app/src/inspect/status_cmd.rs` | 6 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-app/src/migrate/mod.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-app/src/read/mod.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-app/src/replay/mod.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-app/src/write/mod.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-artifacts/src/integrity/hash.rs` | 7 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-core/src/analysis/effects.rs` | 8 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-core/src/graph/model.rs` | 7 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-core/src/graph/resources.rs` | 8 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-core/src/graph/topology.rs` | 7 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-core/src/pipeline/resolve.rs` | 7 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/adapters/api.rs` | 3 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/adapters/builtins/const_value.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/adapters/builtins/container.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/adapters/builtins/mod.rs` | 3 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/adapters/builtins/shell.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/adapters/contract.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/adapters/mod.rs` | 7 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/artifacts/manifest.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/artifacts/mod.rs` | 3 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/artifacts/verifier.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/artifacts/writer.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/backend/fake.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/backend/local_process.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/backend/mod.rs` | 4 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/builtins/const_adapter.rs` | 3 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/builtins/container_adapter.rs` | 3 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/builtins/mod.rs` | 9 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/builtins/shell_adapter.rs` | 3 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/cache/key.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/cache/lineage.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/cache/proof.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/cache/store.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/diagnostics/events.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/diagnostics/ids.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/diagnostics/mod.rs` | 3 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/diagnostics/timeline.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/error/classify.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/error/codes.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/error/mod.rs` | 2 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/internal/clock.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/internal/control/api.rs` | 3 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/internal/control/config.rs` | 3 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/internal/control/runtime.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/internal/control/selectors.rs` | 3 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/internal/io.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/internal/mod.rs` | 3 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/internal/selectors.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/policy/evaluator.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/policy/trace.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/replay/diff.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/replay/explain.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/replay/mod.rs` | 3 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/replay/verifier.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/runtime_core/execution/context.rs` | 7 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/runtime_core/execution/flow.rs` | 5 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/runtime_core/execution/node_result.rs` | 3 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/runtime_core/execution/run_context.rs` | 3 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/runtime_core/mod.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/runtime_core/state/mod.rs` | 2 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/runtime_core/state/node_state.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dag-runtime/src/runtime_core/state/run_state.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dev-dag/src/cli.rs` | 4 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dev-dag/src/policy/mod.rs` | 9 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dev-dag/src/repo/mod.rs` | 2 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dev-dag/src/report/mod.rs` | 2 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dev-dag/src/suites/check.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dev-dag/src/suites/docs.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
| `crates/bijux-dev-dag/src/suites/test.rs` | 1 | keep if boundary-significant; otherwise inline on next touch |
