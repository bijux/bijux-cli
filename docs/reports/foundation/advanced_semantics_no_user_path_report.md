# Advanced Semantics Without Concrete User-Facing Path

All speculative modules listed in `configs/policy/advanced_semantics_governance.json` are marked `user_facing_path=false`.

This confirms speculative semantics are quarantined and not shipped as default product surfaces.
