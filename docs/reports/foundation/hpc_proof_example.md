# HPC Reproducible Run Proof Example

## Example summary

- backend origin: `hpc` (slurm contract)
- run identity: stable
- resource fingerprint: stable across compared runs
- module fingerprint: stable across compared runs
- replay fidelity: `exact`

## Proof statement

This example represents an HPC-origin run where queue/partition/account and module setup remain unchanged, producing exact replay fidelity under contract semantics.
