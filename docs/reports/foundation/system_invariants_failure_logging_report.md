# System Invariants Failure Logging Report

Invariant failures are logged with explicit invariant class, affected run scope,
and deterministic diagnostics for replay, import/export, and runtime fault
paths.
