# Scheduler Overhead Baseline

Scheduler overhead baseline tracks deterministic dispatch cost for:

- 1,000 nodes
- 5,000 nodes
- 10,000 nodes

Primary metric:

- scheduler loop latency per dispatch cycle

Secondary metrics:

- ready-queue maintenance cost
- blocked-by-budget decision overhead

