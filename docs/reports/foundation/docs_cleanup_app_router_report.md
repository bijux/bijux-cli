# Docs Cleanup: App/Router Surfaces

Current result: no stale app/router governance pages requiring deletion.

Reference checks:
- route ownership and hygiene reports under `docs/reports/foundation`
- route contract coverage in `crates/bijux-dev-dag/tests/app_*contracts.rs`
