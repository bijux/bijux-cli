# Operator Command Redundancy Report

Potential overlap families:

- summary-style outputs across `runs`, `inspect`, and dedicated summary renderers
- inspect-style output variants that differ mainly by formatting level
- verification-style outputs across `verify` and supporting diagnostics summaries
- capability/support matrix surfaces across app and dev governance outputs

Current stance: retain stable command IDs while consolidating operator story via compact core command set and contracts.
