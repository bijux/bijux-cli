# Operator Command Merge Candidates Report

Candidate consolidation targets:

- summary output ownership: route all default summaries through canonical inspect summary surface
- inspect output ownership: preserve one concise default and one detailed opt-in path
- verification ownership: preserve `verify` as user-facing authority and keep deeper reports in governance surfaces
- capability matrix ownership: keep app `capabilities` as operator entrypoint and dev reports as supporting evidence
