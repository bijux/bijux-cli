# System Invariants Coverage Report

System invariant coverage spans execution, replay, diff semantics, scheduler
fairness, identity, and backend equivalence, with successful/failed/partial run
paths included.
