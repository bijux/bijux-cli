# Diff Speed Baseline

Diff speed baseline tracks semantic run-diff latency for:

- 100 run-pair comparisons
- 1,000 run-pair comparisons
- 10,000 run-pair comparisons

Primary metric:

- median comparison latency per run-pair

Secondary metrics:

- p95 latency
- memory overhead

