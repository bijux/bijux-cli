# Benchmark Minimal Set Report

Minimal set is represented by five compact packs:

- graph identity and canonicalization
- replay diff and proof
- artifact hash and store
- history inspect and operator flows
- runtime scheduler hot paths

Governance source: `configs/policy/benchmark_signal_governance.json`.
