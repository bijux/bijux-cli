# Schema Single Source Audit

- canonical schema definitions live under `configs/schema/`
- docs/spec schema pages must reference canonical schema files, not duplicate JSON structure
- contract tests must validate schema references remain aligned
