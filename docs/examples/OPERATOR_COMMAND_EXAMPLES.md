# Operator Command Examples

```bash
dag replay --json <run_dir> --out <runs_dir> --prove
dag diff --json <run_a> <run_b> --explain
dag runs inspect --json <run_id> --root <runs_dir>
dag export --json <run_dir> --out <bundle.json> --with-files
dag verify --json <run_dir> --strict
```

