# Sequence example

A longer sequence that stresses ordering:

```bash
bijux config set mode strict
bijux plugins list
bijux plugins uninstall demo
bijux plugins install ./demo_plugin
bijux plugins info demo
bijux config unset mode
```
