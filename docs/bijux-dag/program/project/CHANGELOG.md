# Changelog

All notable changes for the repository are tracked here.

## Unreleased

- Enforce workspace-wide lint policy with strict deny-level clippy settings.
- Expand control-plane suite and command surface in `bijux-dev` (`bijux-dev-dag` binary).
- Add repository docs and governance index for architecture, security, release, and compatibility policy.
- Add runtime and CLI contract test coverage for execution, cache, policy, and exit-code behavior.

## Historical notes

- Initial 0.1 milestone: foundation for deterministic DAG execution and compatibility fixtures.
