# Config Minimized Cases

This directory contains retained `.env` payloads replayed by
`tests/integration/cli/config/config_fuzz_regressions.rs`.

- Each file is a raw config payload written to a temporary config file during replay.
- Keep samples minimized and readable.
- Store only retained regressions here.
