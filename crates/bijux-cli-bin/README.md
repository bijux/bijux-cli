# bijux-cli-bin

Rust binary entrypoint for the new `bijux` foundation.

## Boundary
- Depends on orchestration crates and renders final output.
- Must not contain business logic that belongs in `bijux-cli-core`.
