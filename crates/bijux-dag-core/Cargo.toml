[package]
name = "bijux-dag-core"
version.workspace = true
description = "Deterministic DAG kernel for graph parsing, validation, canonicalization, planning, and identity."
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage = "https://bijux.io/bijux-core/bijux-dag/"
documentation = "https://bijux.io/bijux-core/bijux-dag/packages/bijux-dag-core/"
rust-version.workspace = true
readme = "README.md"
keywords = ["bijux", "dag", "graph", "planning", "validation"]
categories = ["data-structures", "development-tools", "parser-implementations"]

[lints]
workspace = true

[features]
default = []
experimental-public-api = []

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
sha2 = { workspace = true }
hex = { workspace = true }
unicode-normalization = "0.1"

[dev-dependencies]
tempfile = { workspace = true }
criterion = "0.5"
serde_yaml = "0.9"

[[bench]]
name = "micro_parse_validate"
harness = false
