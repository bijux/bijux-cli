//! CLI domain types are centralized in `commands` during incremental extraction.
//! This module is the stable integration point for long-term CLI ownership.

pub(crate) const CONTROL_PLANE_NAME: &str = "bijux-dev-dag";
