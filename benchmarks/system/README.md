# System benchmarks

System benchmarks execute end-to-end workflow commands and record run-artifact-aware timings.

Scope:

- execute-local
- replay
- import/export
- manifest-finalize
- cache lookup behavior
