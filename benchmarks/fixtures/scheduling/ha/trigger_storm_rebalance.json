{
  "scenario_id": "trigger-storm-rebalance",
  "replicas": ["replica-a", "replica-b", "replica-c"],
  "queue_shards": ["shard-0", "shard-1", "shard-2"],
  "trigger_storm_size": 2000,
  "expected": {
    "queue_ownership_transferred": true,
    "submission_sequence_preserved": true
  }
}
