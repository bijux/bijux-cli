# Microbenchmarks

Microbenchmarks are criterion-based crate-level benchmarks.

Scope:

- parse
- validate
- topology and fingerprint building blocks

These are not full workflow execution benchmarks.
