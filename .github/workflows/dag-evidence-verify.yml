name: DAG Evidence Verify

on:
  pull_request:
    paths:
      - "crates/bijux-dag-*/**"
      - "crates/bijux-dev/**"
      - "configs/dag/**"
      - "makes/*.mk"
      - "docs/bijux-dag/**"
      - "evidence/dag/**"
      - ".github/workflows/dag-*"
  push:
    branches: [main]
    paths:
      - "crates/bijux-dag-*/**"
      - "crates/bijux-dev/**"
      - "configs/dag/**"
      - "makes/*.mk"
      - "docs/bijux-dag/**"
      - "evidence/dag/**"
      - ".github/workflows/dag-*"

jobs:
  evidence-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9 # v1
        with:
          toolchain: 1.85.0
      - name: verify evidence foundation
        run: cargo run -p bijux-dev --bin bijux-dev-dag -- verify evidence-release-set
      - name: run battle-first trust subset
        continue-on-error: true
        run: |
          cargo run -p bijux-dev --bin bijux-dev-dag -- verify evidence-battle
          cargo run -p bijux-dev --bin bijux-dev-dag -- verify evidence-cache
          cargo run -p bijux-dev --bin bijux-dev-dag -- verify evidence-replay
          cargo run -p bijux-dev --bin bijux-dev-dag -- verify evidence-consumers
      - name: verify release evidence set
        run: cargo run -p bijux-dev --bin bijux-dev-dag -- verify evidence-release-set
      - name: run release-first trust subset
        run: |
          cargo run -p bijux-dev --bin bijux-dev-dag -- verify evidence-release-set
          cargo run -p bijux-dev --bin bijux-dev-dag -- repo release-evidence-report
      - name: generate evidence summary reports
        run: cargo run -p bijux-dev --bin bijux-dev-dag -- repo evidence-summary-report
      - name: generate scheduler profile report from runtime fixtures
        run: |
          cargo run -p bijux-dev --bin generate_scheduler_profile_report
          git diff --exit-code docs/reports/foundation/scheduler_profile_report.json
      - name: generate runtime adapter and backend scope reports
        run: |
          cargo run -p bijux-dev --bin generate_runtime_adapter_scope_reports
          git diff --exit-code \
            docs/reports/foundation/runtime_adapter_surface_inventory.md \
            docs/reports/foundation/backend_capability_matrix.md \
            docs/reports/foundation/backend_support_matrix.md \
            docs/reports/foundation/unsupported_capability_approximations_report.md \
            docs/reports/foundation/implemented_backend_surfaces_report.md \
            docs/reports/foundation/simulated_backend_surfaces_report.md
      - name: generate app route and response coverage reports
        run: |
          cargo run -p bijux-dev --bin generate_app_route_coverage_reports
          git diff --exit-code \
            docs/reports/foundation/app_route_coverage_by_command_family.md \
            docs/reports/foundation/app_response_schema_coverage_by_command_family.md
      - name: run graph identity fast regression suite
        run: cargo test -p bijux-dag-core --test graph_identity_property_contracts
      - name: generate graph identity decomposition reports
        run: |
          cargo run -p bijux-dev --bin generate_graph_identity_reports
          git diff --exit-code \
            docs/reports/foundation/graph_identity_decomposition_report.json \
            docs/spec/GRAPH_IDENTITY_FIELD_IMPACT.md
      - name: verify shell completion surfaces
        run: cargo test -p bijux-dag-cli --test contract_surface completions_generation_supports_all_supported_shells -- --exact
      - name: upload reports
        uses: actions/upload-artifact@v4
        with:
          name: evidence-verify-reports
          path: artifacts/reports
          if-no-files-found: warn
