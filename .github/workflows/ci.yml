# SSOT NOTICE: This file is generated by bijux-std standards sync.
# Source of truth: bijux-std/.github/standards/repo-config.manifest.json and bijux-std/shared/bijux-gh/workflows/.
# Do not edit this generated copy directly.

name: "repo / ci"
on:
  push:
    branches:
      - main
  pull_request:
concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: true
permissions:
  contents: read
  actions: read
  pull-requests: read
env:
  PIP_DISABLE_PIP_VERSION_CHECK: 1
  PYTHONUNBUFFERED: 1
  BIJUXCLI_API_GUARD: 1
  RUST_TOOLCHAIN_VERSION: 1.85.0
jobs:
  policy_gate:
    name: policy-prerequisites
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
      - name: "Wait for policy and standards prerequisites"
        shell: bash
        env:
          GITHUB_TOKEN: "${{ github.token }}"
        run:
          |
            set -euo pipefail
            python3 .github/scripts/check_workflow_prerequisites.py
  fmt:
    name: Formatting
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: "Check out the repository"
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
      - name: "Set up Python"
        uses: actions/setup-python@83679a892e2d95755f2dac6acb0bfd1e9ac5d548
        with:
          python-version: 3.11
          cache: pip
          cache-dependency-path: crates/bijux-cli-python/pyproject.toml
      - name: "Set up Rust"
        uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
          components: rustfmt
      - name: "Restore the Rust cache"
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
      - name: "Run formatting checks"
        run: "make gh-fmt"
    needs: policy_gate
  lint:
    name: Lint
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: "Check out the repository"
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
      - name: "Set up Python"
        uses: actions/setup-python@83679a892e2d95755f2dac6acb0bfd1e9ac5d548
        with:
          python-version: 3.11
          cache: pip
          cache-dependency-path: crates/bijux-cli-python/pyproject.toml
      - name: "Set up Rust"
        uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
          components: clippy
      - name: "Restore the Rust cache"
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
      - name: "Run lint checks"
        run: "make gh-lint"
    needs: policy_gate
  security:
    name: Security
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: "Check out the repository"
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
      - name: "Set up Python"
        uses: actions/setup-python@83679a892e2d95755f2dac6acb0bfd1e9ac5d548
        with:
          python-version: 3.11
          cache: pip
          cache-dependency-path: crates/bijux-cli-python/pyproject.toml
      - name: "Set up Rust"
        uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
      - name: "Restore the Rust cache"
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
      - name: "Install Rust security tools"
        run: "make gh-security-install-rust-tools"
      - name: "Run security checks"
        run: "make gh-security"
    needs: policy_gate
  test:
    name: "Tests (py${{ matrix.python-version }})"
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - 3.11
          - 3.12
    steps:
      - name: "Check out the repository"
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
      - name: "Set up Python"
        uses: actions/setup-python@83679a892e2d95755f2dac6acb0bfd1e9ac5d548
        with:
          python-version: "${{ matrix.python-version }}"
          cache: pip
          cache-dependency-path: crates/bijux-cli-python/pyproject.toml
      - name: "Set up Rust"
        uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
      - name: "Restore the Rust cache"
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
      - name: "Install cargo-nextest"
        run: "make gh-test-install-rust-tools"
      - name: "Run test suites"
        run: "make gh-test"
    needs: policy_gate
