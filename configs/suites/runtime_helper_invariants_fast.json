{
  "id": "runtime-helper-invariants-fast",
  "description": "Fast direct suite for runtime scheduler, scheduler workload, state machine, semantics, and planner analysis helper invariants.",
  "commands": [
    "cargo test -p bijux-dag-runtime --test runtime_execution_helper_expansion_contracts",
    "cargo test -p bijux-dag-runtime --test runtime_scheduler_state_machine_invariants_contracts",
    "cargo test -p bijux-dag-runtime --test scheduler_workload_contracts",
    "cargo test -p bijux-dag-runtime --test runtime_semantics_contracts",
    "cargo test -p bijux-dag-runtime --test planner_analysis_contract"
  ]
}
