{
  "id": "terminology-consistency-verification",
  "description": "Verification suite for vocabulary registry integrity, canonical term usage, and scope-honest naming surfaces.",
  "commands": [
    "cargo test -p bijux-dev-dag --test vocabulary_scope_honesty_421_440_contracts",
    "cargo test -p bijux-dag-app --test help_surface_contracts",
    "cargo test -p bijux-dev-dag --test runtime_overreach_contracts"
  ]
}
