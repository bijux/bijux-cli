{
  "id": "fixture-helpers-fast",
  "description": "Fast suite for shared fixture loader helpers and duplicate-loader governance.",
  "commands": [
    "cargo test -p bijux-dag-testkit --test fixture_loader_contracts",
    "cargo test -p bijux-dev-dag --test fixture_loader_governance_contracts"
  ]
}
